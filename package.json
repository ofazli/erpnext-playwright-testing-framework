{
  "name": "erpnext-testing-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:start": "docker compose -f compose.dev.yaml up -d",
    "docker:reset": "docker compose -f compose.dev.yaml down --volumes --remove-orphans && docker compose -f compose.dev.yaml up -d",
    "rundocker": "docker compose -f compose.dev.yaml down --volumes --rmi local --remove-orphans && docker compose -f compose.dev.yaml pull && docker compose -f compose.dev.yaml up -d --force-recreate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
